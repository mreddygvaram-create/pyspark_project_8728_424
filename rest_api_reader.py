from pyspark.sql import SparkSession\nimport requests\n\n# Create a Spark session\nspark = SparkSession.builder.appName('REST API Reader').getOrCreate()\n\n# Function to fetch data from the REST API\ndef fetch_data(url):\n    response = requests.get(url)\n    if response.status_code == 200:\n        return response.json()\n    else:\n        raise Exception(f'Error fetching data: {response.status_code}')\n\n# URL of the REST API\napi_url = 'https://api.example.com/data'  # Replace with your REST API URL\n\n# Read the data from the API\ndata = fetch_data(api_url)\n\n# Convert the JSON data to a Spark DataFrame\ndf = spark.read.json(spark.sparkContext.parallelize([data]))\n\n# Show the DataFrame\ndf.show()\n